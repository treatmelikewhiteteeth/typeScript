{
  // Visit https://aka.ms/tsconfig to read more about this file
  "compilerOptions": {
    // File Layout
    "rootDir": "./src",
    "outDir": "./dist",

    // Environment Settings
    // See also https://aka.ms/tsconfig/module
    "module": "nodenext", // Node.js의 최신 ES Modules 규칙을 따른다.
    "target": "esnext", // TypeScriipt가 최신 JS 기능을 거의 그대로 유지하도록 한다.
    "types": [], // 빈 배열로 설정함면 명시적으로 import된 타입만 포함하게 하여 빌드 속도와 제어력을 높인다.
    // For nodejs:
    // "lib": ["esnext"],
    // "types": ["node"],
    // and npm install -D @types/node

    // Other Outputs
    /* 컴파일된 .js 파일 옆에 원본 .ts 파일과의 매핑 정보를 담은 .js.map 파일을 생성한다. */
    "sourceMap": true,
    // TypeScript 파일을 컴파일 할 때 타입ㅂ 정의 파일(.d.ts)을 생성한다.
    // 주로 라이브러리를 개발하여 타입 정보와 함께 배포할 때 사용된다.
    "declaration": true,
    // 생성된 .d.ts 파일에 대한 소스 맵 파일을 생성한다..
    // 디버깅에 도움을 준다.
    "declarationMap": true,

    /* Stricter Typechecking Options 엄격한 타입 검사 옵션
       객체의 인덱스 시그니처(객체명[키])를 통해 접근할 때, 키가 실제로 존재하는지 확인하지 않고 접근하면
       'undifined'가 될 수 있음을 경고한다. */
    "noUncheckedIndexedAccess": true,
    // 선택적 프로퍼티(Optional Property ?가 붙은 속성)를 명시적으로 'undefined'로 지정해야 한다.
    "exactOptionalPropertyTypes": true,

    // Style Options
    // "noImplicitReturns": true,
    // "noImplicitOverride": true,
    // "noUnusedLocals": true,
    // "noUnusedParameters": true,
    // "noFallthroughCasesInSwitch": true,
    // "noPropertyAccessFromIndexSignature": true,

    // Recommended Options
    "strict": true,
    "jsx": "react-jsx",
    "verbatimModuleSyntax": true,
    "isolatedModules": true,
    "noUncheckedSideEffectImports": true,
    "moduleDetection": "force",
    "skipLibCheck": true,
  }
}
